import React from 'react'

function Button({label}) {
  return (
    <button className=' bg-[#4c8bf0] text-[#fff] font-bold block w-max py-4 px-12 rounded-full'>{label}</button>
  )
}

export default Button;